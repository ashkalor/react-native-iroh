name: Build Android

permissions:
  contents: read

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/android-build.yml"
      - "example/android/**"
      - "nitrogen/generated/shared/**"
      - "nitrogen/generated/android/**"
      - "cpp/**"
      - "android/**"
      - "rust/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - ".cargo/**"
      - "scripts/**"
      - "**/bun.lock"
      - "**/react-native.config.js"
      - "**/nitro.json"
  pull_request:
    paths:
      - ".github/workflows/android-build.yml"
      - "example/android/**"
      - "**/nitrogen/generated/shared/**"
      - "**/nitrogen/generated/android/**"
      - "cpp/**"
      - "android/**"
      - "rust/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - ".cargo/**"
      - "scripts/**"
      - "**/bun.lock"
      - "**/react-native.config.js"
      - "**/nitro.json"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  # All four ABIs shipped by the library. The example app defaults to a
  # subset (see example/android/gradle.properties), so CI overrides it.
  ABIS: arm64-v8a,armeabi-v7a,x86_64,x86

jobs:
  build:
    name: Build Android (4 ABIs) + Example App
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # The full build needs ~12GB (4 rust release targets with fat LTO,
      # cargo registry, gradle trees) but a stock runner only has ~14GB
      # free; a previous run died with ENOSPC. Drop preinstalled toolsets
      # this job never uses. The Android SDK/NDK and node must stay.
      - name: Free disk space
        run: |
          set -uo pipefail
          df -h /
          sudo rm -rf \
            /usr/share/dotnet \
            /usr/local/.ghcup \
            /opt/ghc \
            /usr/share/swift \
            /usr/local/share/powershell \
            /usr/local/share/chromium \
            /opt/hostedtoolcache/CodeQL \
            || true
          sudo docker image prune --all --force > /dev/null 2>&1 || true
          df -h /

      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies (bun)
        run: bun install --frozen-lockfile

      - name: Setup JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: "zulu"
          java-version: "17"

      # The nitrogen-generated CMake invokes `cargo build --release --target
      # <triple>` per ABI, so all four Android rust-std components must be
      # installed on the runner.
      - name: Install Rust toolchain (Android targets)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android,armv7-linux-androideabi,x86_64-linux-android,i686-linux-android

      - name: Cache cargo (registry + target)
        uses: Swatinem/rust-cache@v2

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('example/android/**/*.gradle*', 'android/**/*.gradle*') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      # Integration check: the example app must build against the library.
      # Runs on the app's default ABI subset (arm64-v8a + x86_64, see
      # example/android/gradle.properties) - all four ABIs are covered by
      # the release leg below, and keeping this leg small saves disk and
      # minutes. The rust side is always compiled in release mode.
      - name: Build example app (assembleDebug, default ABIs)
        working-directory: example/android
        run: ./gradlew assembleDebug --no-daemon --build-cache

      # The debug leg's intermediates (unstripped debug .so, per-ABI copies
      # of the 130-150MB rust .a, APK merge dirs) are dead weight for the
      # release leg; reclaim them to keep the runner off the ENOSPC cliff.
      - name: Reclaim debug build space
        run: |
          set -uo pipefail
          rm -rf example/android/app/build android/.cxx/Debug android/build/intermediates
          df -h /

      # Release variant of the library itself: this is where the
      # distributable per-ABI libIroh.so artifacts come from. Cargo
      # artifacts for arm64-v8a/x86_64 are shared with the debug leg
      # (same target dir and env), so only armv7/i686 rust plus the C++
      # glue are built here.
      - name: Build library (assembleRelease, all ABIs)
        working-directory: example/android
        run: ./gradlew :react-native-iroh:assembleRelease --no-daemon --build-cache -PreactNativeArchitectures=${{ env.ABIS }}

      - name: Collect artifacts
        run: |
          set -euo pipefail
          out=dist/android
          libdir=android/build/intermediates/stripped_native_libs/release/stripReleaseDebugSymbols/out/lib
          for abi in ${ABIS//,/ }; do
            mkdir -p "$out/$abi"
            cp "$libdir/$abi/libIroh.so" "$out/$abi/libIroh.so"
          done
          cp android/build/outputs/aar/*-release.aar "$out/" 2>/dev/null || cp android/build/outputs/aar/*.aar "$out/"
          find "$out" -type f | sort

      # Google Play requires 16KB page-size support for arm64 (and NDK 27
      # only provides it when ANDROID_SUPPORT_FLEXIBLE_PAGE_SIZES is on, see
      # android/build.gradle). Assert every LOAD segment is 0x4000-aligned.
      - name: Assert 16KB page-size alignment (arm64-v8a)
        run: |
          set -euo pipefail
          so=dist/android/arm64-v8a/libIroh.so
          readelf -lW "$so" | awk '/LOAD/ {print $NF}' | tee /tmp/aligns.txt
          if grep -qvx '0x4000' /tmp/aligns.txt; then
            echo "error: $so has a LOAD segment not aligned to 16KB (0x4000)" >&2
            exit 1
          fi
          echo "OK: all LOAD segments of $so are 16KB-aligned"

      - name: Size report
        run: |
          set -euo pipefail
          report=dist/android/SIZES.md
          {
            echo "### Android artifact sizes (release, stripped)"
            echo ""
            echo "| Artifact | Size |"
            echo "| -------- | ---- |"
            for abi in ${ABIS//,/ }; do
              so=dist/android/$abi/libIroh.so
              echo "| $abi/libIroh.so | $(du -h "$so" | cut -f1) ($(stat -c%s "$so") bytes) |"
            done
            for aar in dist/android/*.aar; do
              echo "| $(basename "$aar") | $(du -h "$aar" | cut -f1) ($(stat -c%s "$aar") bytes) |"
            done
          } | tee "$report" >> "$GITHUB_STEP_SUMMARY"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: android-libs
          path: dist/android
          if-no-files-found: error
