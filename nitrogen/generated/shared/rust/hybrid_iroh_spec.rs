//
// hybrid_iroh_spec.rs
// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
// https://github.com/mrousavy/nitro
// Copyright © Marc Rousavy @ Margelo
//
#![allow(
    non_camel_case_types,
    non_snake_case,
    dead_code,
    unused_imports,
    clippy::needless_return,
    clippy::redundant_closure,
    clippy::new_without_default,
    clippy::useless_conversion
)]

use super::endpoint_config::EndpointConfig;
use super::nitro_buffer::NitroBuffer;
use super::stream_framing::StreamFraming;
use crate::*;

/// Implement this trait to create a Rust-backed HybridObject for `Iroh`.
///
/// After implementing, provide a factory function for registration:
/// ```rust
/// #[unsafe(no_mangle)]
/// pub extern "C" fn create_HybridIrohSpec() -> *mut std::ffi::c_void {
///     let obj: std::sync::Arc<dyn HybridIrohSpec> = std::sync::Arc::new(MyIroh::new());
///     Box::into_raw(Box::new(obj)) as *mut std::ffi::c_void
/// }
/// ```
///
/// Note: The factory returns a `Box<Arc<dyn HybridIrohSpec>>` — the outer Box
/// provides a stable thin pointer for C++, while the inner Arc enables shared ownership.
///
/// All methods take `&self` (shared reference) because the C++ bridge may call
/// methods concurrently from multiple threads. Use interior mutability
/// (`Mutex`, `RwLock`, `AtomicXxx`, etc.) for any mutable state.
pub trait HybridIrohSpec: Send + Sync {
    // Properties

    // Methods
    fn create_endpoint(
        &self,
        config: EndpointConfig,
        promise: Box<dyn FnOnce(Result<f64, String>) + Send>,
    );
    fn endpoint_id(&self, endpoint: f64) -> Result<String, String>;
    fn is_endpoint_open(&self, endpoint: f64) -> Result<bool, String>;
    fn endpoint_addr(&self, endpoint: f64) -> Result<String, String>;
    fn watch_addr(
        &self,
        endpoint: f64,
        on_start: Box<dyn Fn(f64) + Send + Sync>,
        on_change: Box<dyn Fn(String) + Send + Sync>,
    ) -> Result<(), String>;
    fn stop_watch_addr(&self, watch_id: f64) -> Result<(), String>;
    fn remote_info(
        &self,
        endpoint: f64,
        remote_id: String,
        promise: Box<dyn FnOnce(Result<String, String>) + Send>,
    );
    fn endpoint_online(
        &self,
        endpoint: f64,
        timeout_ms: f64,
        promise: Box<dyn FnOnce(Result<(), String>) + Send>,
    );
    fn close_endpoint(&self, endpoint: f64, promise: Box<dyn FnOnce(Result<(), String>) + Send>);
    fn share_blob(
        &self,
        endpoint: f64,
        path: String,
        promise: Box<dyn FnOnce(Result<String, String>) + Send>,
    );
    fn download_blob(
        &self,
        endpoint: f64,
        ticket: String,
        dest_path: String,
        on_start: Box<dyn Fn(f64) + Send + Sync>,
        on_progress: Box<dyn Fn(f64) + Send + Sync>,
        promise: Box<dyn FnOnce(Result<(), String>) + Send>,
    );
    fn cancel_download(&self, transfer_id: f64) -> Result<(), String>;
    fn share_collection(
        &self,
        endpoint: f64,
        paths_joined: String,
        promise: Box<dyn FnOnce(Result<String, String>) + Send>,
    );
    fn collection_manifest(
        &self,
        endpoint: f64,
        ticket: String,
        promise: Box<dyn FnOnce(Result<String, String>) + Send>,
    );
    fn parse_ticket(&self, ticket: String) -> Result<String, String>;
    fn gossip_subscribe(
        &self,
        endpoint: f64,
        topic: String,
        bootstrap_joined: String,
        on_start: Box<dyn Fn(f64) + Send + Sync>,
        on_message: Box<dyn Fn(String) + Send + Sync>,
        on_neighbor: Box<dyn Fn(String) + Send + Sync>,
    ) -> Result<(), String>;
    fn gossip_broadcast(
        &self,
        sub_id: f64,
        payload: String,
        promise: Box<dyn FnOnce(Result<(), String>) + Send>,
    );
    fn gossip_unsubscribe(&self, sub_id: f64) -> Result<(), String>;
    fn stream_listen(
        &self,
        endpoint: f64,
        alpn: String,
        on_connection: Box<dyn Fn(String) + Send + Sync>,
        on_close: Box<dyn Fn(String) + Send + Sync>,
    ) -> Result<f64, String>;
    fn stop_stream_listen(&self, listener_id: f64) -> Result<(), String>;
    fn stream_connect(
        &self,
        endpoint: f64,
        remote_addr: String,
        alpn: String,
        promise: Box<dyn FnOnce(Result<f64, String>) + Send>,
    );
    fn stream_connection_subscribe(
        &self,
        connection_id: f64,
        framing: StreamFraming,
        on_stream: Box<dyn Fn(f64) + Send + Sync>,
        on_close: Box<dyn Fn(String) + Send + Sync>,
    ) -> Result<(), String>;
    fn stream_open_stream(
        &self,
        connection_id: f64,
        promise: Box<dyn FnOnce(Result<f64, String>) + Send>,
    );
    fn stream_close_connection(&self, connection_id: f64) -> Result<(), String>;
    fn stream_subscribe(
        &self,
        stream_id: f64,
        on_data: Box<dyn Fn(NitroBuffer) + Send + Sync>,
        on_close: Box<dyn Fn(String) + Send + Sync>,
    ) -> Result<(), String>;
    fn stream_send(
        &self,
        stream_id: f64,
        data: NitroBuffer,
        promise: Box<dyn FnOnce(Result<(), String>) + Send>,
    );
    fn stream_close(&self, stream_id: f64) -> Result<(), String>;
    fn authors_default(
        &self,
        endpoint: f64,
        promise: Box<dyn FnOnce(Result<String, String>) + Send>,
    );
    fn authors_create(
        &self,
        endpoint: f64,
        promise: Box<dyn FnOnce(Result<String, String>) + Send>,
    );
    fn authors_list(&self, endpoint: f64, promise: Box<dyn FnOnce(Result<String, String>) + Send>);
    fn authors_import(
        &self,
        endpoint: f64,
        secret_key: String,
        promise: Box<dyn FnOnce(Result<String, String>) + Send>,
    );
    fn docs_create(&self, endpoint: f64, promise: Box<dyn FnOnce(Result<String, String>) + Send>);
    fn docs_open(
        &self,
        endpoint: f64,
        namespace_id: String,
        promise: Box<dyn FnOnce(Result<bool, String>) + Send>,
    );
    fn docs_import(
        &self,
        endpoint: f64,
        ticket: String,
        promise: Box<dyn FnOnce(Result<String, String>) + Send>,
    );
    fn docs_list(&self, endpoint: f64, promise: Box<dyn FnOnce(Result<String, String>) + Send>);
    fn docs_drop(
        &self,
        endpoint: f64,
        namespace_id: String,
        promise: Box<dyn FnOnce(Result<(), String>) + Send>,
    );
    fn docs_set_bytes(
        &self,
        endpoint: f64,
        namespace_id: String,
        author_id: String,
        key: String,
        value: NitroBuffer,
        promise: Box<dyn FnOnce(Result<String, String>) + Send>,
    );
    fn docs_get_exact(
        &self,
        endpoint: f64,
        namespace_id: String,
        author_id: String,
        key: String,
        promise: Box<dyn FnOnce(Result<String, String>) + Send>,
    );
    fn docs_get_many(
        &self,
        endpoint: f64,
        namespace_id: String,
        query_json: String,
        promise: Box<dyn FnOnce(Result<String, String>) + Send>,
    );
    fn docs_delete_prefix(
        &self,
        endpoint: f64,
        namespace_id: String,
        author_id: String,
        prefix: String,
        promise: Box<dyn FnOnce(Result<f64, String>) + Send>,
    );
    fn docs_share(
        &self,
        endpoint: f64,
        namespace_id: String,
        mode: String,
        promise: Box<dyn FnOnce(Result<String, String>) + Send>,
    );
    fn docs_get_content(
        &self,
        endpoint: f64,
        hash: String,
        promise: Box<dyn FnOnce(Result<NitroBuffer, String>) + Send>,
    );
    fn docs_subscribe(
        &self,
        endpoint: f64,
        namespace_id: String,
        on_start: Box<dyn Fn(f64) + Send + Sync>,
        on_event: Box<dyn Fn(String) + Send + Sync>,
        on_close: Box<dyn Fn(String) + Send + Sync>,
    ) -> Result<(), String>;
    fn docs_unsubscribe(&self, sub_id: f64) -> Result<(), String>;
    fn docs_start_sync(
        &self,
        endpoint: f64,
        namespace_id: String,
        peers_joined: String,
        promise: Box<dyn FnOnce(Result<(), String>) + Send>,
    );
    fn docs_leave(
        &self,
        endpoint: f64,
        namespace_id: String,
        promise: Box<dyn FnOnce(Result<(), String>) + Send>,
    );
    fn parse_doc_ticket(&self, ticket: String) -> Result<String, String>;

    /// Return the size of any external heap allocations, in bytes.
    /// This is used to inform the JavaScript GC about native memory pressure.
    fn memory_size(&self) -> usize {
        0
    }
}

// FFI shims for C++ bridge

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_create_endpoint(
    ptr: *mut std::ffi::c_void,
    config: *mut std::ffi::c_void,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void, f64),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void, f64),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<f64, String>) {
                self.armed = false;
                match __result {
                    Ok(__value) => unsafe { (self.resolve)(self.userdata, __value) },
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<f64, String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __config = {
                #[repr(C)]
                struct __Struct {
                    preset: i32,
                    blobStoreDir: *mut std::ffi::c_void,
                    docs: *mut std::ffi::c_void,
                    docsStoreDir: *mut std::ffi::c_void,
                    relayMode: *mut std::ffi::c_void,
                    alpns: *mut std::ffi::c_void,
                }
                let __s = *Box::from_raw(config as *mut __Struct);
                super::endpoint_config::EndpointConfig {
                    preset: super::network_preset::NetworkPreset::from_i32(__s.preset)
                        .unwrap_or_else(|| {
                            panic!("[Nitro] Invalid NetworkPreset discriminant: {}", __s.preset)
                        }),
                    blob_store_dir: {
                        #[repr(C)]
                        struct __Opt {
                            has_value: u8,
                            value: *const std::ffi::c_char,
                        }
                        let __s = *Box::from_raw(__s.blobStoreDir as *mut __Opt);
                        if __s.has_value != 0 {
                            Some(
                                std::ffi::CStr::from_ptr(__s.value)
                                    .to_string_lossy()
                                    .into_owned(),
                            )
                        } else {
                            None
                        }
                    },
                    docs: {
                        #[repr(C)]
                        struct __Opt {
                            has_value: u8,
                            value: bool,
                        }
                        let __s = *Box::from_raw(__s.docs as *mut __Opt);
                        if __s.has_value != 0 {
                            Some(__s.value)
                        } else {
                            None
                        }
                    },
                    docs_store_dir: {
                        #[repr(C)]
                        struct __Opt {
                            has_value: u8,
                            value: *const std::ffi::c_char,
                        }
                        let __s = *Box::from_raw(__s.docsStoreDir as *mut __Opt);
                        if __s.has_value != 0 {
                            Some(
                                std::ffi::CStr::from_ptr(__s.value)
                                    .to_string_lossy()
                                    .into_owned(),
                            )
                        } else {
                            None
                        }
                    },
                    relay_mode: {
                        #[repr(C)]
                        struct __Opt {
                            has_value: u8,
                            value: *const std::ffi::c_char,
                        }
                        let __s = *Box::from_raw(__s.relayMode as *mut __Opt);
                        if __s.has_value != 0 {
                            Some(
                                std::ffi::CStr::from_ptr(__s.value)
                                    .to_string_lossy()
                                    .into_owned(),
                            )
                        } else {
                            None
                        }
                    },
                    alpns: {
                        #[repr(C)]
                        struct __Opt {
                            has_value: u8,
                            value: *const std::ffi::c_char,
                        }
                        let __s = *Box::from_raw(__s.alpns as *mut __Opt);
                        if __s.has_value != 0 {
                            Some(
                                std::ffi::CStr::from_ptr(__s.value)
                                    .to_string_lossy()
                                    .into_owned(),
                            )
                        } else {
                            None
                        }
                    },
                }
            };
            obj.create_endpoint(__config, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_endpoint_id(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
) -> __FfiResult_cstr {
    // NOTE: AssertUnwindSafe is used because UnwindSafe cannot be required on the trait
    // without making it viral across all implementations. If a panic occurs mid-mutation,
    // the object's internal state may be inconsistent on subsequent calls.
    unsafe {
        match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            obj.endpoint_id(endpoint).map(|__value| {
                let __s = __value.replace('\0', "");
                std::ffi::CString::new(__s).unwrap_or_default().into_raw()
            })
        })) {
            Ok(Ok(__value)) => __FfiResult_cstr {
                is_ok: 1,
                error: std::ptr::null_mut(),
                value: __value,
            },
            Ok(Err(__err)) => __FfiResult_cstr {
                is_ok: 0,
                error: {
                    let __s = __err.replace('\0', "");
                    std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                },
                value: std::mem::zeroed(),
            },
            // SAFETY: value is intentionally zeroed on error — C++ checks is_ok before reading it.
            Err(__panic) => __FfiResult_cstr {
                is_ok: 0,
                error: crate::__nitro_panic_to_cstring(__panic),
                value: std::mem::zeroed(),
            },
        }
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_is_endpoint_open(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
) -> __FfiResult_bool {
    // NOTE: AssertUnwindSafe is used because UnwindSafe cannot be required on the trait
    // without making it viral across all implementations. If a panic occurs mid-mutation,
    // the object's internal state may be inconsistent on subsequent calls.
    unsafe {
        match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            obj.is_endpoint_open(endpoint)
        })) {
            Ok(Ok(__value)) => __FfiResult_bool {
                is_ok: 1,
                error: std::ptr::null_mut(),
                value: __value,
            },
            Ok(Err(__err)) => __FfiResult_bool {
                is_ok: 0,
                error: {
                    let __s = __err.replace('\0', "");
                    std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                },
                value: std::mem::zeroed(),
            },
            // SAFETY: value is intentionally zeroed on error — C++ checks is_ok before reading it.
            Err(__panic) => __FfiResult_bool {
                is_ok: 0,
                error: crate::__nitro_panic_to_cstring(__panic),
                value: std::mem::zeroed(),
            },
        }
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_endpoint_addr(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
) -> __FfiResult_cstr {
    // NOTE: AssertUnwindSafe is used because UnwindSafe cannot be required on the trait
    // without making it viral across all implementations. If a panic occurs mid-mutation,
    // the object's internal state may be inconsistent on subsequent calls.
    unsafe {
        match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            obj.endpoint_addr(endpoint).map(|__value| {
                let __s = __value.replace('\0', "");
                std::ffi::CString::new(__s).unwrap_or_default().into_raw()
            })
        })) {
            Ok(Ok(__value)) => __FfiResult_cstr {
                is_ok: 1,
                error: std::ptr::null_mut(),
                value: __value,
            },
            Ok(Err(__err)) => __FfiResult_cstr {
                is_ok: 0,
                error: {
                    let __s = __err.replace('\0', "");
                    std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                },
                value: std::mem::zeroed(),
            },
            // SAFETY: value is intentionally zeroed on error — C++ checks is_ok before reading it.
            Err(__panic) => __FfiResult_cstr {
                is_ok: 0,
                error: crate::__nitro_panic_to_cstring(__panic),
                value: std::mem::zeroed(),
            },
        }
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_watch_addr(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    on_start: *mut std::ffi::c_void,
    on_change: *mut std::ffi::c_void,
) -> __FfiResult_void {
    // NOTE: AssertUnwindSafe is used because UnwindSafe cannot be required on the trait
    // without making it viral across all implementations. If a panic occurs mid-mutation,
    // the object's internal state may be inconsistent on subsequent calls.
    unsafe {
        match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __on_start = {
                let __wrapper =
                    Box::from_raw(on_start as *mut super::func_void_double::Func_void_double);
                let __cb: Box<dyn Fn(f64) + Send + Sync> =
                    Box::new(move |__p0: f64| __wrapper.call(__p0));
                __cb
            };
            let __on_change = {
                let __wrapper = Box::from_raw(
                    on_change as *mut super::func_void_std__string::Func_void_std__string,
                );
                let __cb: Box<dyn Fn(String) + Send + Sync> =
                    Box::new(move |__p0: String| __wrapper.call(__p0));
                __cb
            };
            obj.watch_addr(endpoint, __on_start, __on_change)
        })) {
            Ok(Ok(_)) => __FfiResult_void {
                is_ok: 1,
                error: std::ptr::null_mut(),
            },
            Ok(Err(__err)) => __FfiResult_void {
                is_ok: 0,
                error: {
                    let __s = __err.replace('\0', "");
                    std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                },
            },
            Err(__panic) => __FfiResult_void {
                is_ok: 0,
                error: crate::__nitro_panic_to_cstring(__panic),
            },
        }
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_stop_watch_addr(
    ptr: *mut std::ffi::c_void,
    watch_id: f64,
) -> __FfiResult_void {
    // NOTE: AssertUnwindSafe is used because UnwindSafe cannot be required on the trait
    // without making it viral across all implementations. If a panic occurs mid-mutation,
    // the object's internal state may be inconsistent on subsequent calls.
    unsafe {
        match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            obj.stop_watch_addr(watch_id)
        })) {
            Ok(Ok(_)) => __FfiResult_void {
                is_ok: 1,
                error: std::ptr::null_mut(),
            },
            Ok(Err(__err)) => __FfiResult_void {
                is_ok: 0,
                error: {
                    let __s = __err.replace('\0', "");
                    std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                },
            },
            Err(__panic) => __FfiResult_void {
                is_ok: 0,
                error: crate::__nitro_panic_to_cstring(__panic),
            },
        }
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_remote_info(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    remote_id: *const std::ffi::c_char,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<String, String>) {
                self.armed = false;
                match __result {
                    Ok(__value) => {
                        let __ffi_value = {
                            let __s = __value.replace('\0', "");
                            std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                        };
                        unsafe { (self.resolve)(self.userdata, __ffi_value) };
                    }
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<String, String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __remote_id = std::ffi::CStr::from_ptr(remote_id)
                .to_string_lossy()
                .into_owned();
            obj.remote_info(endpoint, __remote_id, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_endpoint_online(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    timeout_ms: f64,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<(), String>) {
                self.armed = false;
                match __result {
                    Ok(()) => unsafe { (self.resolve)(self.userdata) },
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<(), String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            obj.endpoint_online(endpoint, timeout_ms, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_close_endpoint(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<(), String>) {
                self.armed = false;
                match __result {
                    Ok(()) => unsafe { (self.resolve)(self.userdata) },
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<(), String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            obj.close_endpoint(endpoint, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_share_blob(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    path: *const std::ffi::c_char,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<String, String>) {
                self.armed = false;
                match __result {
                    Ok(__value) => {
                        let __ffi_value = {
                            let __s = __value.replace('\0', "");
                            std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                        };
                        unsafe { (self.resolve)(self.userdata, __ffi_value) };
                    }
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<String, String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __path = std::ffi::CStr::from_ptr(path)
                .to_string_lossy()
                .into_owned();
            obj.share_blob(endpoint, __path, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_download_blob(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    ticket: *const std::ffi::c_char,
    dest_path: *const std::ffi::c_char,
    on_start: *mut std::ffi::c_void,
    on_progress: *mut std::ffi::c_void,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<(), String>) {
                self.armed = false;
                match __result {
                    Ok(()) => unsafe { (self.resolve)(self.userdata) },
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<(), String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __ticket = std::ffi::CStr::from_ptr(ticket)
                .to_string_lossy()
                .into_owned();
            let __dest_path = std::ffi::CStr::from_ptr(dest_path)
                .to_string_lossy()
                .into_owned();
            let __on_start = {
                let __wrapper =
                    Box::from_raw(on_start as *mut super::func_void_double::Func_void_double);
                let __cb: Box<dyn Fn(f64) + Send + Sync> =
                    Box::new(move |__p0: f64| __wrapper.call(__p0));
                __cb
            };
            let __on_progress = {
                let __wrapper =
                    Box::from_raw(on_progress as *mut super::func_void_double::Func_void_double);
                let __cb: Box<dyn Fn(f64) + Send + Sync> =
                    Box::new(move |__p0: f64| __wrapper.call(__p0));
                __cb
            };
            obj.download_blob(
                endpoint,
                __ticket,
                __dest_path,
                __on_start,
                __on_progress,
                __complete,
            );
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_cancel_download(
    ptr: *mut std::ffi::c_void,
    transfer_id: f64,
) -> __FfiResult_void {
    // NOTE: AssertUnwindSafe is used because UnwindSafe cannot be required on the trait
    // without making it viral across all implementations. If a panic occurs mid-mutation,
    // the object's internal state may be inconsistent on subsequent calls.
    unsafe {
        match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            obj.cancel_download(transfer_id)
        })) {
            Ok(Ok(_)) => __FfiResult_void {
                is_ok: 1,
                error: std::ptr::null_mut(),
            },
            Ok(Err(__err)) => __FfiResult_void {
                is_ok: 0,
                error: {
                    let __s = __err.replace('\0', "");
                    std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                },
            },
            Err(__panic) => __FfiResult_void {
                is_ok: 0,
                error: crate::__nitro_panic_to_cstring(__panic),
            },
        }
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_share_collection(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    paths_joined: *const std::ffi::c_char,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<String, String>) {
                self.armed = false;
                match __result {
                    Ok(__value) => {
                        let __ffi_value = {
                            let __s = __value.replace('\0', "");
                            std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                        };
                        unsafe { (self.resolve)(self.userdata, __ffi_value) };
                    }
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<String, String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __paths_joined = std::ffi::CStr::from_ptr(paths_joined)
                .to_string_lossy()
                .into_owned();
            obj.share_collection(endpoint, __paths_joined, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_collection_manifest(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    ticket: *const std::ffi::c_char,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<String, String>) {
                self.armed = false;
                match __result {
                    Ok(__value) => {
                        let __ffi_value = {
                            let __s = __value.replace('\0', "");
                            std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                        };
                        unsafe { (self.resolve)(self.userdata, __ffi_value) };
                    }
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<String, String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __ticket = std::ffi::CStr::from_ptr(ticket)
                .to_string_lossy()
                .into_owned();
            obj.collection_manifest(endpoint, __ticket, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_parse_ticket(
    ptr: *mut std::ffi::c_void,
    ticket: *const std::ffi::c_char,
) -> __FfiResult_cstr {
    // NOTE: AssertUnwindSafe is used because UnwindSafe cannot be required on the trait
    // without making it viral across all implementations. If a panic occurs mid-mutation,
    // the object's internal state may be inconsistent on subsequent calls.
    unsafe {
        match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __ticket = std::ffi::CStr::from_ptr(ticket)
                .to_string_lossy()
                .into_owned();
            obj.parse_ticket(__ticket).map(|__value| {
                let __s = __value.replace('\0', "");
                std::ffi::CString::new(__s).unwrap_or_default().into_raw()
            })
        })) {
            Ok(Ok(__value)) => __FfiResult_cstr {
                is_ok: 1,
                error: std::ptr::null_mut(),
                value: __value,
            },
            Ok(Err(__err)) => __FfiResult_cstr {
                is_ok: 0,
                error: {
                    let __s = __err.replace('\0', "");
                    std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                },
                value: std::mem::zeroed(),
            },
            // SAFETY: value is intentionally zeroed on error — C++ checks is_ok before reading it.
            Err(__panic) => __FfiResult_cstr {
                is_ok: 0,
                error: crate::__nitro_panic_to_cstring(__panic),
                value: std::mem::zeroed(),
            },
        }
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_gossip_subscribe(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    topic: *const std::ffi::c_char,
    bootstrap_joined: *const std::ffi::c_char,
    on_start: *mut std::ffi::c_void,
    on_message: *mut std::ffi::c_void,
    on_neighbor: *mut std::ffi::c_void,
) -> __FfiResult_void {
    // NOTE: AssertUnwindSafe is used because UnwindSafe cannot be required on the trait
    // without making it viral across all implementations. If a panic occurs mid-mutation,
    // the object's internal state may be inconsistent on subsequent calls.
    unsafe {
        match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __topic = std::ffi::CStr::from_ptr(topic)
                .to_string_lossy()
                .into_owned();
            let __bootstrap_joined = std::ffi::CStr::from_ptr(bootstrap_joined)
                .to_string_lossy()
                .into_owned();
            let __on_start = {
                let __wrapper =
                    Box::from_raw(on_start as *mut super::func_void_double::Func_void_double);
                let __cb: Box<dyn Fn(f64) + Send + Sync> =
                    Box::new(move |__p0: f64| __wrapper.call(__p0));
                __cb
            };
            let __on_message = {
                let __wrapper = Box::from_raw(
                    on_message as *mut super::func_void_std__string::Func_void_std__string,
                );
                let __cb: Box<dyn Fn(String) + Send + Sync> =
                    Box::new(move |__p0: String| __wrapper.call(__p0));
                __cb
            };
            let __on_neighbor = {
                let __wrapper = Box::from_raw(
                    on_neighbor as *mut super::func_void_std__string::Func_void_std__string,
                );
                let __cb: Box<dyn Fn(String) + Send + Sync> =
                    Box::new(move |__p0: String| __wrapper.call(__p0));
                __cb
            };
            obj.gossip_subscribe(
                endpoint,
                __topic,
                __bootstrap_joined,
                __on_start,
                __on_message,
                __on_neighbor,
            )
        })) {
            Ok(Ok(_)) => __FfiResult_void {
                is_ok: 1,
                error: std::ptr::null_mut(),
            },
            Ok(Err(__err)) => __FfiResult_void {
                is_ok: 0,
                error: {
                    let __s = __err.replace('\0', "");
                    std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                },
            },
            Err(__panic) => __FfiResult_void {
                is_ok: 0,
                error: crate::__nitro_panic_to_cstring(__panic),
            },
        }
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_gossip_broadcast(
    ptr: *mut std::ffi::c_void,
    sub_id: f64,
    payload: *const std::ffi::c_char,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<(), String>) {
                self.armed = false;
                match __result {
                    Ok(()) => unsafe { (self.resolve)(self.userdata) },
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<(), String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __payload = std::ffi::CStr::from_ptr(payload)
                .to_string_lossy()
                .into_owned();
            obj.gossip_broadcast(sub_id, __payload, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_gossip_unsubscribe(
    ptr: *mut std::ffi::c_void,
    sub_id: f64,
) -> __FfiResult_void {
    // NOTE: AssertUnwindSafe is used because UnwindSafe cannot be required on the trait
    // without making it viral across all implementations. If a panic occurs mid-mutation,
    // the object's internal state may be inconsistent on subsequent calls.
    unsafe {
        match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            obj.gossip_unsubscribe(sub_id)
        })) {
            Ok(Ok(_)) => __FfiResult_void {
                is_ok: 1,
                error: std::ptr::null_mut(),
            },
            Ok(Err(__err)) => __FfiResult_void {
                is_ok: 0,
                error: {
                    let __s = __err.replace('\0', "");
                    std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                },
            },
            Err(__panic) => __FfiResult_void {
                is_ok: 0,
                error: crate::__nitro_panic_to_cstring(__panic),
            },
        }
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_stream_listen(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    alpn: *const std::ffi::c_char,
    on_connection: *mut std::ffi::c_void,
    on_close: *mut std::ffi::c_void,
) -> __FfiResult_f64 {
    // NOTE: AssertUnwindSafe is used because UnwindSafe cannot be required on the trait
    // without making it viral across all implementations. If a panic occurs mid-mutation,
    // the object's internal state may be inconsistent on subsequent calls.
    unsafe {
        match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __alpn = std::ffi::CStr::from_ptr(alpn)
                .to_string_lossy()
                .into_owned();
            let __on_connection = {
                let __wrapper = Box::from_raw(
                    on_connection as *mut super::func_void_std__string::Func_void_std__string,
                );
                let __cb: Box<dyn Fn(String) + Send + Sync> =
                    Box::new(move |__p0: String| __wrapper.call(__p0));
                __cb
            };
            let __on_close = {
                let __wrapper = Box::from_raw(
                    on_close as *mut super::func_void_std__string::Func_void_std__string,
                );
                let __cb: Box<dyn Fn(String) + Send + Sync> =
                    Box::new(move |__p0: String| __wrapper.call(__p0));
                __cb
            };
            obj.stream_listen(endpoint, __alpn, __on_connection, __on_close)
        })) {
            Ok(Ok(__value)) => __FfiResult_f64 {
                is_ok: 1,
                error: std::ptr::null_mut(),
                value: __value,
            },
            Ok(Err(__err)) => __FfiResult_f64 {
                is_ok: 0,
                error: {
                    let __s = __err.replace('\0', "");
                    std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                },
                value: std::mem::zeroed(),
            },
            // SAFETY: value is intentionally zeroed on error — C++ checks is_ok before reading it.
            Err(__panic) => __FfiResult_f64 {
                is_ok: 0,
                error: crate::__nitro_panic_to_cstring(__panic),
                value: std::mem::zeroed(),
            },
        }
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_stop_stream_listen(
    ptr: *mut std::ffi::c_void,
    listener_id: f64,
) -> __FfiResult_void {
    // NOTE: AssertUnwindSafe is used because UnwindSafe cannot be required on the trait
    // without making it viral across all implementations. If a panic occurs mid-mutation,
    // the object's internal state may be inconsistent on subsequent calls.
    unsafe {
        match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            obj.stop_stream_listen(listener_id)
        })) {
            Ok(Ok(_)) => __FfiResult_void {
                is_ok: 1,
                error: std::ptr::null_mut(),
            },
            Ok(Err(__err)) => __FfiResult_void {
                is_ok: 0,
                error: {
                    let __s = __err.replace('\0', "");
                    std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                },
            },
            Err(__panic) => __FfiResult_void {
                is_ok: 0,
                error: crate::__nitro_panic_to_cstring(__panic),
            },
        }
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_stream_connect(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    remote_addr: *const std::ffi::c_char,
    alpn: *const std::ffi::c_char,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void, f64),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void, f64),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<f64, String>) {
                self.armed = false;
                match __result {
                    Ok(__value) => unsafe { (self.resolve)(self.userdata, __value) },
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<f64, String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __remote_addr = std::ffi::CStr::from_ptr(remote_addr)
                .to_string_lossy()
                .into_owned();
            let __alpn = std::ffi::CStr::from_ptr(alpn)
                .to_string_lossy()
                .into_owned();
            obj.stream_connect(endpoint, __remote_addr, __alpn, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_stream_connection_subscribe(
    ptr: *mut std::ffi::c_void,
    connection_id: f64,
    framing: i32,
    on_stream: *mut std::ffi::c_void,
    on_close: *mut std::ffi::c_void,
) -> __FfiResult_void {
    // NOTE: AssertUnwindSafe is used because UnwindSafe cannot be required on the trait
    // without making it viral across all implementations. If a panic occurs mid-mutation,
    // the object's internal state may be inconsistent on subsequent calls.
    unsafe {
        match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __framing =
                super::stream_framing::StreamFraming::from_i32(framing).unwrap_or_else(|| {
                    panic!("[Nitro] Invalid StreamFraming discriminant: {}", framing)
                });
            let __on_stream = {
                let __wrapper =
                    Box::from_raw(on_stream as *mut super::func_void_double::Func_void_double);
                let __cb: Box<dyn Fn(f64) + Send + Sync> =
                    Box::new(move |__p0: f64| __wrapper.call(__p0));
                __cb
            };
            let __on_close = {
                let __wrapper = Box::from_raw(
                    on_close as *mut super::func_void_std__string::Func_void_std__string,
                );
                let __cb: Box<dyn Fn(String) + Send + Sync> =
                    Box::new(move |__p0: String| __wrapper.call(__p0));
                __cb
            };
            obj.stream_connection_subscribe(connection_id, __framing, __on_stream, __on_close)
        })) {
            Ok(Ok(_)) => __FfiResult_void {
                is_ok: 1,
                error: std::ptr::null_mut(),
            },
            Ok(Err(__err)) => __FfiResult_void {
                is_ok: 0,
                error: {
                    let __s = __err.replace('\0', "");
                    std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                },
            },
            Err(__panic) => __FfiResult_void {
                is_ok: 0,
                error: crate::__nitro_panic_to_cstring(__panic),
            },
        }
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_stream_open_stream(
    ptr: *mut std::ffi::c_void,
    connection_id: f64,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void, f64),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void, f64),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<f64, String>) {
                self.armed = false;
                match __result {
                    Ok(__value) => unsafe { (self.resolve)(self.userdata, __value) },
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<f64, String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            obj.stream_open_stream(connection_id, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_stream_close_connection(
    ptr: *mut std::ffi::c_void,
    connection_id: f64,
) -> __FfiResult_void {
    // NOTE: AssertUnwindSafe is used because UnwindSafe cannot be required on the trait
    // without making it viral across all implementations. If a panic occurs mid-mutation,
    // the object's internal state may be inconsistent on subsequent calls.
    unsafe {
        match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            obj.stream_close_connection(connection_id)
        })) {
            Ok(Ok(_)) => __FfiResult_void {
                is_ok: 1,
                error: std::ptr::null_mut(),
            },
            Ok(Err(__err)) => __FfiResult_void {
                is_ok: 0,
                error: {
                    let __s = __err.replace('\0', "");
                    std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                },
            },
            Err(__panic) => __FfiResult_void {
                is_ok: 0,
                error: crate::__nitro_panic_to_cstring(__panic),
            },
        }
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_stream_subscribe(
    ptr: *mut std::ffi::c_void,
    stream_id: f64,
    on_data: *mut std::ffi::c_void,
    on_close: *mut std::ffi::c_void,
) -> __FfiResult_void {
    // NOTE: AssertUnwindSafe is used because UnwindSafe cannot be required on the trait
    // without making it viral across all implementations. If a panic occurs mid-mutation,
    // the object's internal state may be inconsistent on subsequent calls.
    unsafe {
        match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __on_data = {
                let __wrapper = Box::from_raw(on_data as *mut super::func_void_std__shared_ptr_array_buffer_::Func_void_std__shared_ptr_ArrayBuffer_);
                let __cb: Box<dyn Fn(NitroBuffer) + Send + Sync> =
                    Box::new(move |__p0: NitroBuffer| __wrapper.call(__p0));
                __cb
            };
            let __on_close = {
                let __wrapper = Box::from_raw(
                    on_close as *mut super::func_void_std__string::Func_void_std__string,
                );
                let __cb: Box<dyn Fn(String) + Send + Sync> =
                    Box::new(move |__p0: String| __wrapper.call(__p0));
                __cb
            };
            obj.stream_subscribe(stream_id, __on_data, __on_close)
        })) {
            Ok(Ok(_)) => __FfiResult_void {
                is_ok: 1,
                error: std::ptr::null_mut(),
            },
            Ok(Err(__err)) => __FfiResult_void {
                is_ok: 0,
                error: {
                    let __s = __err.replace('\0', "");
                    std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                },
            },
            Err(__panic) => __FfiResult_void {
                is_ok: 0,
                error: crate::__nitro_panic_to_cstring(__panic),
            },
        }
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_stream_send(
    ptr: *mut std::ffi::c_void,
    stream_id: f64,
    data: *mut std::ffi::c_void,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<(), String>) {
                self.armed = false;
                match __result {
                    Ok(()) => unsafe { (self.resolve)(self.userdata) },
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<(), String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __data = *Box::from_raw(data as *mut super::nitro_buffer::NitroBuffer);
            obj.stream_send(stream_id, __data, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_stream_close(
    ptr: *mut std::ffi::c_void,
    stream_id: f64,
) -> __FfiResult_void {
    // NOTE: AssertUnwindSafe is used because UnwindSafe cannot be required on the trait
    // without making it viral across all implementations. If a panic occurs mid-mutation,
    // the object's internal state may be inconsistent on subsequent calls.
    unsafe {
        match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            obj.stream_close(stream_id)
        })) {
            Ok(Ok(_)) => __FfiResult_void {
                is_ok: 1,
                error: std::ptr::null_mut(),
            },
            Ok(Err(__err)) => __FfiResult_void {
                is_ok: 0,
                error: {
                    let __s = __err.replace('\0', "");
                    std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                },
            },
            Err(__panic) => __FfiResult_void {
                is_ok: 0,
                error: crate::__nitro_panic_to_cstring(__panic),
            },
        }
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_authors_default(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<String, String>) {
                self.armed = false;
                match __result {
                    Ok(__value) => {
                        let __ffi_value = {
                            let __s = __value.replace('\0', "");
                            std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                        };
                        unsafe { (self.resolve)(self.userdata, __ffi_value) };
                    }
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<String, String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            obj.authors_default(endpoint, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_authors_create(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<String, String>) {
                self.armed = false;
                match __result {
                    Ok(__value) => {
                        let __ffi_value = {
                            let __s = __value.replace('\0', "");
                            std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                        };
                        unsafe { (self.resolve)(self.userdata, __ffi_value) };
                    }
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<String, String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            obj.authors_create(endpoint, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_authors_list(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<String, String>) {
                self.armed = false;
                match __result {
                    Ok(__value) => {
                        let __ffi_value = {
                            let __s = __value.replace('\0', "");
                            std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                        };
                        unsafe { (self.resolve)(self.userdata, __ffi_value) };
                    }
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<String, String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            obj.authors_list(endpoint, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_authors_import(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    secret_key: *const std::ffi::c_char,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<String, String>) {
                self.armed = false;
                match __result {
                    Ok(__value) => {
                        let __ffi_value = {
                            let __s = __value.replace('\0', "");
                            std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                        };
                        unsafe { (self.resolve)(self.userdata, __ffi_value) };
                    }
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<String, String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __secret_key = std::ffi::CStr::from_ptr(secret_key)
                .to_string_lossy()
                .into_owned();
            obj.authors_import(endpoint, __secret_key, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_docs_create(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<String, String>) {
                self.armed = false;
                match __result {
                    Ok(__value) => {
                        let __ffi_value = {
                            let __s = __value.replace('\0', "");
                            std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                        };
                        unsafe { (self.resolve)(self.userdata, __ffi_value) };
                    }
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<String, String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            obj.docs_create(endpoint, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_docs_open(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    namespace_id: *const std::ffi::c_char,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void, bool),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void, bool),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<bool, String>) {
                self.armed = false;
                match __result {
                    Ok(__value) => unsafe { (self.resolve)(self.userdata, __value) },
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<bool, String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __namespace_id = std::ffi::CStr::from_ptr(namespace_id)
                .to_string_lossy()
                .into_owned();
            obj.docs_open(endpoint, __namespace_id, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_docs_import(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    ticket: *const std::ffi::c_char,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<String, String>) {
                self.armed = false;
                match __result {
                    Ok(__value) => {
                        let __ffi_value = {
                            let __s = __value.replace('\0', "");
                            std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                        };
                        unsafe { (self.resolve)(self.userdata, __ffi_value) };
                    }
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<String, String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __ticket = std::ffi::CStr::from_ptr(ticket)
                .to_string_lossy()
                .into_owned();
            obj.docs_import(endpoint, __ticket, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_docs_list(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<String, String>) {
                self.armed = false;
                match __result {
                    Ok(__value) => {
                        let __ffi_value = {
                            let __s = __value.replace('\0', "");
                            std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                        };
                        unsafe { (self.resolve)(self.userdata, __ffi_value) };
                    }
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<String, String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            obj.docs_list(endpoint, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_docs_drop(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    namespace_id: *const std::ffi::c_char,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<(), String>) {
                self.armed = false;
                match __result {
                    Ok(()) => unsafe { (self.resolve)(self.userdata) },
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<(), String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __namespace_id = std::ffi::CStr::from_ptr(namespace_id)
                .to_string_lossy()
                .into_owned();
            obj.docs_drop(endpoint, __namespace_id, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_docs_set_bytes(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    namespace_id: *const std::ffi::c_char,
    author_id: *const std::ffi::c_char,
    key: *const std::ffi::c_char,
    value: *mut std::ffi::c_void,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<String, String>) {
                self.armed = false;
                match __result {
                    Ok(__value) => {
                        let __ffi_value = {
                            let __s = __value.replace('\0', "");
                            std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                        };
                        unsafe { (self.resolve)(self.userdata, __ffi_value) };
                    }
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<String, String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __namespace_id = std::ffi::CStr::from_ptr(namespace_id)
                .to_string_lossy()
                .into_owned();
            let __author_id = std::ffi::CStr::from_ptr(author_id)
                .to_string_lossy()
                .into_owned();
            let __key = std::ffi::CStr::from_ptr(key).to_string_lossy().into_owned();
            let __value = *Box::from_raw(value as *mut super::nitro_buffer::NitroBuffer);
            obj.docs_set_bytes(
                endpoint,
                __namespace_id,
                __author_id,
                __key,
                __value,
                __complete,
            );
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_docs_get_exact(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    namespace_id: *const std::ffi::c_char,
    author_id: *const std::ffi::c_char,
    key: *const std::ffi::c_char,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<String, String>) {
                self.armed = false;
                match __result {
                    Ok(__value) => {
                        let __ffi_value = {
                            let __s = __value.replace('\0', "");
                            std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                        };
                        unsafe { (self.resolve)(self.userdata, __ffi_value) };
                    }
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<String, String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __namespace_id = std::ffi::CStr::from_ptr(namespace_id)
                .to_string_lossy()
                .into_owned();
            let __author_id = std::ffi::CStr::from_ptr(author_id)
                .to_string_lossy()
                .into_owned();
            let __key = std::ffi::CStr::from_ptr(key).to_string_lossy().into_owned();
            obj.docs_get_exact(endpoint, __namespace_id, __author_id, __key, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_docs_get_many(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    namespace_id: *const std::ffi::c_char,
    query_json: *const std::ffi::c_char,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<String, String>) {
                self.armed = false;
                match __result {
                    Ok(__value) => {
                        let __ffi_value = {
                            let __s = __value.replace('\0', "");
                            std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                        };
                        unsafe { (self.resolve)(self.userdata, __ffi_value) };
                    }
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<String, String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __namespace_id = std::ffi::CStr::from_ptr(namespace_id)
                .to_string_lossy()
                .into_owned();
            let __query_json = std::ffi::CStr::from_ptr(query_json)
                .to_string_lossy()
                .into_owned();
            obj.docs_get_many(endpoint, __namespace_id, __query_json, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_docs_delete_prefix(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    namespace_id: *const std::ffi::c_char,
    author_id: *const std::ffi::c_char,
    prefix: *const std::ffi::c_char,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void, f64),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void, f64),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<f64, String>) {
                self.armed = false;
                match __result {
                    Ok(__value) => unsafe { (self.resolve)(self.userdata, __value) },
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<f64, String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __namespace_id = std::ffi::CStr::from_ptr(namespace_id)
                .to_string_lossy()
                .into_owned();
            let __author_id = std::ffi::CStr::from_ptr(author_id)
                .to_string_lossy()
                .into_owned();
            let __prefix = std::ffi::CStr::from_ptr(prefix)
                .to_string_lossy()
                .into_owned();
            obj.docs_delete_prefix(endpoint, __namespace_id, __author_id, __prefix, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_docs_share(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    namespace_id: *const std::ffi::c_char,
    mode: *const std::ffi::c_char,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *const std::ffi::c_char),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<String, String>) {
                self.armed = false;
                match __result {
                    Ok(__value) => {
                        let __ffi_value = {
                            let __s = __value.replace('\0', "");
                            std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                        };
                        unsafe { (self.resolve)(self.userdata, __ffi_value) };
                    }
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<String, String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __namespace_id = std::ffi::CStr::from_ptr(namespace_id)
                .to_string_lossy()
                .into_owned();
            let __mode = std::ffi::CStr::from_ptr(mode)
                .to_string_lossy()
                .into_owned();
            obj.docs_share(endpoint, __namespace_id, __mode, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_docs_get_content(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    hash: *const std::ffi::c_char,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_void),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_void),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<NitroBuffer, String>) {
                self.armed = false;
                match __result {
                    Ok(__value) => {
                        let __ffi_value = Box::into_raw(Box::new(__value)) as *mut std::ffi::c_void;
                        unsafe { (self.resolve)(self.userdata, __ffi_value) };
                    }
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<NitroBuffer, String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __hash = std::ffi::CStr::from_ptr(hash)
                .to_string_lossy()
                .into_owned();
            obj.docs_get_content(endpoint, __hash, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_docs_subscribe(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    namespace_id: *const std::ffi::c_char,
    on_start: *mut std::ffi::c_void,
    on_event: *mut std::ffi::c_void,
    on_close: *mut std::ffi::c_void,
) -> __FfiResult_void {
    // NOTE: AssertUnwindSafe is used because UnwindSafe cannot be required on the trait
    // without making it viral across all implementations. If a panic occurs mid-mutation,
    // the object's internal state may be inconsistent on subsequent calls.
    unsafe {
        match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __namespace_id = std::ffi::CStr::from_ptr(namespace_id)
                .to_string_lossy()
                .into_owned();
            let __on_start = {
                let __wrapper =
                    Box::from_raw(on_start as *mut super::func_void_double::Func_void_double);
                let __cb: Box<dyn Fn(f64) + Send + Sync> =
                    Box::new(move |__p0: f64| __wrapper.call(__p0));
                __cb
            };
            let __on_event = {
                let __wrapper = Box::from_raw(
                    on_event as *mut super::func_void_std__string::Func_void_std__string,
                );
                let __cb: Box<dyn Fn(String) + Send + Sync> =
                    Box::new(move |__p0: String| __wrapper.call(__p0));
                __cb
            };
            let __on_close = {
                let __wrapper = Box::from_raw(
                    on_close as *mut super::func_void_std__string::Func_void_std__string,
                );
                let __cb: Box<dyn Fn(String) + Send + Sync> =
                    Box::new(move |__p0: String| __wrapper.call(__p0));
                __cb
            };
            obj.docs_subscribe(endpoint, __namespace_id, __on_start, __on_event, __on_close)
        })) {
            Ok(Ok(_)) => __FfiResult_void {
                is_ok: 1,
                error: std::ptr::null_mut(),
            },
            Ok(Err(__err)) => __FfiResult_void {
                is_ok: 0,
                error: {
                    let __s = __err.replace('\0', "");
                    std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                },
            },
            Err(__panic) => __FfiResult_void {
                is_ok: 0,
                error: crate::__nitro_panic_to_cstring(__panic),
            },
        }
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_docs_unsubscribe(
    ptr: *mut std::ffi::c_void,
    sub_id: f64,
) -> __FfiResult_void {
    // NOTE: AssertUnwindSafe is used because UnwindSafe cannot be required on the trait
    // without making it viral across all implementations. If a panic occurs mid-mutation,
    // the object's internal state may be inconsistent on subsequent calls.
    unsafe {
        match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            obj.docs_unsubscribe(sub_id)
        })) {
            Ok(Ok(_)) => __FfiResult_void {
                is_ok: 1,
                error: std::ptr::null_mut(),
            },
            Ok(Err(__err)) => __FfiResult_void {
                is_ok: 0,
                error: {
                    let __s = __err.replace('\0', "");
                    std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                },
            },
            Err(__panic) => __FfiResult_void {
                is_ok: 0,
                error: crate::__nitro_panic_to_cstring(__panic),
            },
        }
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_docs_start_sync(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    namespace_id: *const std::ffi::c_char,
    peers_joined: *const std::ffi::c_char,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<(), String>) {
                self.armed = false;
                match __result {
                    Ok(()) => unsafe { (self.resolve)(self.userdata) },
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<(), String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __namespace_id = std::ffi::CStr::from_ptr(namespace_id)
                .to_string_lossy()
                .into_owned();
            let __peers_joined = std::ffi::CStr::from_ptr(peers_joined)
                .to_string_lossy()
                .into_owned();
            obj.docs_start_sync(endpoint, __namespace_id, __peers_joined, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_docs_leave(
    ptr: *mut std::ffi::c_void,
    endpoint: f64,
    namespace_id: *const std::ffi::c_char,
    __resolve: unsafe extern "C" fn(*mut std::ffi::c_void),
    __reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
    __userdata: *mut std::ffi::c_void,
) {
    unsafe {
        // Completer: forwards the trait's async result to the C++ resolve/reject
        // callbacks. Fires exactly once; keeps userdata (the C++ Promise) alive
        // until completion; rejects on drop so the Promise never hangs.
        struct __Completer {
            resolve: unsafe extern "C" fn(*mut std::ffi::c_void),
            reject: unsafe extern "C" fn(*mut std::ffi::c_void, *mut std::ffi::c_char),
            userdata: *mut std::ffi::c_void,
            armed: bool,
        }
        // SAFETY: the C++ side guarantees resolve/reject/userdata are valid and
        // may be invoked from another thread.
        unsafe impl Send for __Completer {}
        impl __Completer {
            fn complete(mut self, __result: Result<(), String>) {
                self.armed = false;
                match __result {
                    Ok(()) => unsafe { (self.resolve)(self.userdata) },
                    Err(__err) => {
                        let __s = __err.replace('\0', "");
                        let __c = std::ffi::CString::new(__s).unwrap_or_default().into_raw();
                        unsafe { (self.reject)(self.userdata, __c) };
                    }
                }
            }
        }
        impl Drop for __Completer {
            fn drop(&mut self) {
                if self.armed {
                    let __c = std::ffi::CString::new("Promise dropped without completion")
                        .unwrap_or_default()
                        .into_raw();
                    unsafe { (self.reject)(self.userdata, __c) };
                }
            }
        }
        let __completer = __Completer {
            resolve: __resolve,
            reject: __reject,
            userdata: __userdata,
            armed: true,
        };
        let __complete: Box<dyn FnOnce(Result<(), String>) + Send> =
            Box::new(move |__r| __completer.complete(__r));
        let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(move || {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __namespace_id = std::ffi::CStr::from_ptr(namespace_id)
                .to_string_lossy()
                .into_owned();
            obj.docs_leave(endpoint, __namespace_id, __complete);
        }));
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_parse_doc_ticket(
    ptr: *mut std::ffi::c_void,
    ticket: *const std::ffi::c_char,
) -> __FfiResult_cstr {
    // NOTE: AssertUnwindSafe is used because UnwindSafe cannot be required on the trait
    // without making it viral across all implementations. If a panic occurs mid-mutation,
    // the object's internal state may be inconsistent on subsequent calls.
    unsafe {
        match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
            let __ticket = std::ffi::CStr::from_ptr(ticket)
                .to_string_lossy()
                .into_owned();
            obj.parse_doc_ticket(__ticket).map(|__value| {
                let __s = __value.replace('\0', "");
                std::ffi::CString::new(__s).unwrap_or_default().into_raw()
            })
        })) {
            Ok(Ok(__value)) => __FfiResult_cstr {
                is_ok: 1,
                error: std::ptr::null_mut(),
                value: __value,
            },
            Ok(Err(__err)) => __FfiResult_cstr {
                is_ok: 0,
                error: {
                    let __s = __err.replace('\0', "");
                    std::ffi::CString::new(__s).unwrap_or_default().into_raw()
                },
                value: std::mem::zeroed(),
            },
            // SAFETY: value is intentionally zeroed on error — C++ checks is_ok before reading it.
            Err(__panic) => __FfiResult_cstr {
                is_ok: 0,
                error: crate::__nitro_panic_to_cstring(__panic),
                value: std::mem::zeroed(),
            },
        }
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_memory_size(ptr: *mut std::ffi::c_void) -> usize {
    unsafe {
        let obj = &*(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
        obj.memory_size()
    }
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn HybridIrohSpec_destroy(ptr: *mut std::ffi::c_void) {
    unsafe {
        let _ = Box::from_raw(ptr as *mut std::sync::Arc<dyn HybridIrohSpec>);
    }
}
